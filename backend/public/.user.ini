; Watchizer production PHP overrides (Hostinger reads .user.ini per-directory).
; Placed in public/ so it applies to the web-facing document root.
memory_limit = 512M
max_execution_time = 120
upload_max_filesize = 10M
post_max_size = 12M
max_input_vars = 3000
