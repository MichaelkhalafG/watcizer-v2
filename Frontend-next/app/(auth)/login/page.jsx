// PLACEHOLDER — verifies the (auth) group renders with NO header/footer.
// The real Login page replaces this in Phase 4.
export default function LoginPlaceholder() {
  return (
    <div style={{ padding: 48, textAlign: 'center' }}>
      <h1>Login (placeholder)</h1>
      <p style={{ color: 'rgba(0,0,0,0.55)' }}>
        This route is in the (auth) group — no header, no footer (full-bleed).
      </p>
    </div>
  )
}
